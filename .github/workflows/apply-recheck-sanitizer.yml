name: Apply sanitizer realm recheck

on:
  push:
    branches:
      - fix/audit-tdd-2026-09-05
    paths:
      - '.audit-recheck/**'
      - '.github/workflows/apply-recheck-sanitizer.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
      - name: Apply verified sanitizer patch
        run: |
          set -euo pipefail
          cat .audit-recheck/*.b64 | base64 -d | gzip -d > /tmp/recheck.patch
          git apply --check /tmp/recheck.patch
          git apply /tmp/recheck.patch
          rm -rf .audit-recheck .github/workflows/apply-recheck-sanitizer.yml
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "fix(realm): keep sanitizers in owning document"
          git push
