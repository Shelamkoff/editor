---
title: "Блочный плагин Poll"
outline: deep
editLink: false
---

<!-- Generated by scripts/sync-vitepress-readmes.mjs. Edit the source README.ru.md instead. -->

# Блочный плагин Poll

Опрос с одиночным или множественным выбором и локальными либо предоставленными приложением актуальными результатами.

## Установка и регистрация

```bash
npm install @shelamkoff/rector
```

```js
import { createEditor } from '@shelamkoff/rector'
import { Poll } from '@shelamkoff/rector/plugins/poll'

const editor = createEditor({
  holder: document.querySelector('#editor'),
  plugins: [new Poll()],
})
```

Тип блока — `poll`. Класс также экспортируется общей точкой входа `@shelamkoff/rector/plugins` и может загружаться по типу документа через `@shelamkoff/rector/plugins/async`.

## Данные

```json
{
  "pollId": "release-survey",
  "question": "В какой канал отправить уведомление о выпуске?",
  "type": "single",
  "options": [{ "id": "stable", "text": "Стабильный" }, { "id": "next", "text": "Следующий" }],
  "resultsMode": "afterVote",
  "initialResults": { "total": 0, "options": [{ "id": "stable", "votes": 0 }, { "id": "next", "votes": 0 }] }
}
```

### Поля данных

| Поле | Обязательное | Назначение и ограничения |
| --- | --- | --- |
| `pollId` | с `dataSource` | Непустой идентификатор приложения для удалённого `dataSource`. Автоматически не создаётся. Без него настроенный источник не загружается, а отправка голоса сообщает об ошибке. |
| `question` | да | Непустой вопрос опроса. |
| `type` | да | `single` или `multiple`. |
| `options` | да | Не менее двух вариантов. Каждому нужны уникальный непустой `id` и непустой `text`; идентификаторы сохраняются при изменении текста и порядка. |
| `resultsMode` | да | `always`, `afterVote` или `hidden`; при нормализации по умолчанию `always`. |
| `initialResults` | нет | Необязательный исходный или локальный снимок результатов. Если он передан, поля `total` и `options` обязательны. |
| `initialResults.total` | с `initialResults` | Неотрицательное целое число бюллетеней, используемое как знаменатель процентов до ответа внешнего источника или при локальном голосовании. |
| `initialResults.options` | с `initialResults` | Ровно один элемент `{ id, votes }` для каждого варианта. Число голосов должно быть неотрицательным целым. В опросе с множественным выбором их сумма может превышать `total`. |
| `revision` | во время работы | Необязательная непрозрачная ревизия сервера. `compareRevisions` задаёт порядок, если порядка поступления недостаточно. |
| `voters`, `votersTotal`, `currentUserVote` | во время работы | Необязательные данные результата сервера. Идентификатор голосующего обязателен; имя, разрешённый политикой медиафайлов аватар и выбранные варианты нормализуются. Текущее состояние сервера не записывается в историю отмены и повтора. |

`type` равен `single` или `multiple`, а `resultsMode` — `always`, `afterVote` или `hidden`. Идентификаторы вариантов устойчивы. Без `dataSource` голосование изменяет `initialResults` и входит в историю. С `dataSource` актуальные результаты, сведения о проголосовавших, ревизия и голос текущего пользователя остаются оперативным состоянием и не попадают в документ или историю.

## Конфигурация

Каждый встроенный блочный плагин принимает два параметра владения стилями: `injectStyles?: boolean` по умолчанию равен `true`; укажите `false`, если приложение само включает CSS этого плагина. `css?: string` добавляет URL таблицы стилей приложения после стандартной, а при отключённой стандартной инъекции служит URL замены.

`dataSource` содержит обязательные методы `load` и `vote` и необязательный `subscribe`; каждый получает `AbortSignal`, а `vote` также получает текущую `revision`. Каждый результат должен содержать `total` и по одному элементу `{ id, votes }` для каждого текущего варианта. `total` — число бюллетеней и знаменатель процентов; при множественном выборе сумма процентов может превышать 100%. `compareRevisions(next, current)` упорядочивает непрозрачные строки ревизий, если сервер может прислать обновления не по порядку; без неё разные ревизии применяются в порядке получения. `onError` наблюдает ошибки источника. `maxVoters` ограничивает число сохраняемых сведений о проголосовавших: конечное значение округляется вниз и ограничивается снизу нулём, а при отсутствии или некорректном значении используется `50`.

```js
const poll = new Poll({
  dataSource: {
    async load({ pollId, signal }) {
      return api.getPollResults(pollId, { signal })
    },
    async vote({ pollId, optionIds, revision, signal }) {
      return api.submitPollVote(pollId, { optionIds, revision }, { signal })
    },
    subscribe({ pollId, signal, onUpdate, onError }) {
      const connection = api.subscribeToPoll(pollId, { onUpdate, onError })
      signal.addEventListener('abort', () => connection.close(), { once: true })
      return () => connection.close()
    },
  },
  compareRevisions: (next, current) => Number(next) - Number(current),
  onError(error) {
    console.error('Ошибка источника данных опроса', error)
  },
})
```

`load` получает первый достоверный снимок. `vote` отправляет весь текущий выбор, а не только изменение, и возвращает новый достоверный снимок. `subscribe` передаёт последующие снимки через `onUpdate`; при ошибке соединения вызовите `onError` и верните функцию очистки, которую можно безопасно вызвать повторно. При удалении блока или редактора плагин также прерывает переданный сигнал. Не считайте ревизии числами, если приложение не передало числовую `compareRevisions`, как в примере.


## Возможности

Редактирование вопроса и вариантов; настройка видимости результатов; изменение порядка; локальное голосование; прерывание загрузки и отправки голоса через `AbortSignal`; подписка на обновления; упорядочение редакций; сведения о проголосовавших; защита от повторной отправки и освобождение ресурсов.

## История, жизненный цикл и стили

Действия плагина входят в конвейер команд через предоставленный контекст `mutate()`, поэтому одно завершённое действие создаёт один шаг отмены и повтора. Редактор подсчитывает владельцев объявленных URL стилей. Удаление блока вызывает его метод освобождения ресурсов; `editor.destroy()` освобождает оставшиеся блоки и общие ресурсы.

Не удаляйте контейнер редактора до вызова `editor.destroy()`.

## Вывод документа

Используйте фабричную функцию из `@shelamkoff/rector/renderer/renderers/poll`. Последовательное руководство VitePress описывает проверку данных, миграции, разработку расширений, диагностику, безопасность и стили.
