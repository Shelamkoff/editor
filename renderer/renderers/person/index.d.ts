/**
 * Person/author card block renderer — supports multi-person with carousel
 * @param {string} classPrefix
 * @returns {import('../../types').BlockRenderer<import('../../types').PersonBlock>}
 */
export function createPersonRenderer(classPrefix: string, locale: Record<string, string>): import("../../types").BlockRenderer<import("../../types").PersonBlock>;
